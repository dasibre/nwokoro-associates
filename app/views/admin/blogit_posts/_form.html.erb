<%= semantic_form_for @blogit_post, :url => blogit.posts_url, :html => {:method => :post } do |f| %>
<%= f.inputs do %>
	<%= f.input :title %>
	<%= f.input :body%>
	<% end %>
 <%= f.actions %>
<% end %>